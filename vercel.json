{
    "version": 2,
    "build": [
        {  
            "buildCommand": "npm run build",
            "src": "./server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
           "src": "/(.*)",
           "dest": "/"
        }
        
    ]
}