{
    "version": 2,
    "build": [
        {  
            "buildCommand": "next build",
            "src": "./server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
           "src": "/(.*)",
           "dest": "/"
        }
        
    ]
}